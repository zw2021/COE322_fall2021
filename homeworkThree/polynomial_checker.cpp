#include <iostream>
#include <vector>
#include <iomanip>

using std::setw;
using namespace std;

// a polynomial is NOT proper if:
// the power of the variable is raised to a negative value e.g x^-2
// the power of the variable is raised to a fraction e.g x^1/2

int proper_polynomial(){
    // check if the power of the variable is a non negtative value
    // power of the variable is raised to a fraction

    // check if size of vector is degree n+1
    // e.g cubic function x^3 has 4 elements: x^3 + 0x^2 +0x +0

    REQUIRE()
    return 0;
}
int main() {
    return 0;
}
